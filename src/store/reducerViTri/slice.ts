import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

export const { reducer: viTriReducer, actions: viTriActions } = createSlice({
  name: "reducerViTri",
  initialState,
  reducers: {},

  extraReducers(builder) {},
});
